# Redirige todo HTTP a HTTPS para paladinopropiedades.com.ar y www.paladinopropiedades.com.ar
server {
    listen 80;
    listen [::]:80;
    server_name paladinopropiedades.com.ar www.paladinopropiedades.com.ar;

    # Necesario para Certbot (no tocar)
    location ^~ /.well-known/acme-challenge/ {
        root /var/www/html;
        default_type "text/plain";
    }

    return 301 https://$host$request_uri;
}

# Redirige todo HTTP a HTTPS para los otros dominios
server {
    if ($host = api.paladinopropiedades.com.ar) {
        return 301 https://$host$request_uri;
    } # managed by Certbot

    if ($host = admipa.paladinopropiedades.com.ar) {
        return 301 https://$host$request_uri;
    } # managed by Certbot

    listen 80;
    listen [::]:80;
    server_name admipa.paladinopropiedades.com.ar api.paladinopropiedades.com.ar;
    return 301 https://$host$request_uri;
}

# -------- paladinopropiedades.com.ar -> localhost:3000 --------
server {
    listen 443 ssl;
    listen [::]:443 ssl;
    http2 on;

    server_name paladinopropiedades.com.ar www.paladinopropiedades.com.ar;
    ssl_certificate /etc/letsencrypt/live/admipa.paladinopropiedades.com.ar/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/admipa.paladinopropiedades.com.ar/privkey.pem; # managed by Certbot
    include             /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;

    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade           $http_upgrade;
        proxy_set_header Connection        "upgrade";
        proxy_read_timeout  120s;
        proxy_send_timeout  120s;
    }

}

# -------- admipa.paladinopropiedades.com.ar -> localhost:1337 --------
server {
    listen 443 ssl;
    listen [::]:443 ssl;
    http2 on;

    server_name admipa.paladinopropiedades.com.ar;

    # Snakeoil TEMPORAL
    ssl_certificate /etc/letsencrypt/live/admipa.paladinopropiedades.com.ar/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/admipa.paladinopropiedades.com.ar/privkey.pem; # managed by Certbot
    include             /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;

    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    client_max_body_size 100m;
    location / {
        proxy_pass http://localhost:1337;
        proxy_http_version 1.1;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade           $http_upgrade;
        proxy_set_header Connection        "upgrade";
        proxy_read_timeout  300s;
        proxy_send_timeout  300s;
    }


}

# -------- api.paladinopropiedades.com.ar -> localhost:1337 --------
server {
    listen 443 ssl;
    listen [::]:443 ssl;
    http2 on;

    server_name api.paladinopropiedades.com.ar;

    # Snakeoil TEMPORAL
    ssl_certificate /etc/letsencrypt/live/admipa.paladinopropiedades.com.ar/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/admipa.paladinopropiedades.com.ar/privkey.pem; # managed by Certbot
    include             /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;

    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    client_max_body_size 100m;
    location / {
        proxy_pass http://localhost:1337;
        proxy_http_version 1.1;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade           $http_upgrade;
        proxy_set_header Connection        "upgrade";
        proxy_read_timeout  300s;
        proxy_send_timeout  300s;
    }


}

